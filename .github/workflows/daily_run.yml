name: Daily Expert Alpha Update

on:
  schedule:
    - cron: '30 14 * * *' # 매일 한국시간 밤 11시 30분
  workflow_dispatch: # 수동 실행 버튼

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install pandas requests beautifulsoup4 pytz yfinance

      - name: Run Scraper
        run: python main.py

      # 🔥 [이 부분이 핵심!] 변경된 DB 파일을 저장소에 강제로 밀어넣기
      - name: Commit and Push changes
        run: |
          git config --global user.name "GitHub Action"
          git config --global user.email "action@github.com"
          git add expert_alpha_v3.db
          git diff --quiet && git diff --staged --quiet || (git commit -m "Update DB with latest reports" && git push)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
