name: ExpertAlpha Factory

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 pandas FinanceDataReader  # <-- 제가 이거 추가했습니다!

      - name: Run Factory
        run: python main.py

      - name: Commit and Push DB
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add *.db
          # 변경사항이 있을 때만 커밋하고 푸시합니다.
          git diff --quiet && git diff --staged --quiet || (git commit -m "🚀 [ExpertAlpha] 시세 데이터 업데이트 완료" && git push)
