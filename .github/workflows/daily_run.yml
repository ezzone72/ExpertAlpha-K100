name: ExpertAlpha Factory

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: |
          pip install requests beautifulsoup4 pandas

      - name: Run Factory
        run: python main.py

      - name: Commit and Push DB
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          # 모든 DB 파일을 강제로 다 챙겨서 올립니다.
          git add *.db 
          git commit -m "🚀 [Final] 인간 지표 데이터 적재 완료" || echo "No changes to commit"
          git push
