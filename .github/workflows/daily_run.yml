name: ExpertAlpha-K100 Daily Scraper

on:
  schedule:
    # 한국 시간 밤 11:30 (UTC 14:30)
    - cron: '30 14 * * *'
  workflow_dispatch: # 수동 실행 버튼

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests beautifulsoup4 pandas yfinance pytz schedule

    - name: Run Main Script
      run: |
        export PYTHONPATH=$PYTHONPATH:$(pwd)
        python main.py --update-prices

    - name: Commit and Push DB change
      env:
        # 소장님이 만드신 시크릿 이름 그대로 적용합니다
        TOKEN: ${{ secrets.gh_ffnm_key }}
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        
        if [ -f expert_alpha_v3.db ]; then
          git add expert_alpha_v3.db
          git commit -m "📈 Daily DB Update: $(date +'%Y-%m-%d')" || echo "No changes to commit"
          
          # 토큰을 주소에 태워서 문을 강제로 엽니다
          git push https://x-access-token:${TOKEN}@github.com/ezzone72/ExpertAlpha-K100.git HEAD:main
        else
          echo "DB file not found, skipping push."
        fi
